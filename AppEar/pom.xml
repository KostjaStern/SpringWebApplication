<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.sternkn.spring.multcontext</groupId>
        <artifactId>app-reactor</artifactId>
        <version>0.1</version>
    </parent>

    <artifactId>app-ear</artifactId>
    <name>app-ear</name>
    <description>Spring enterprise application</description>
    <packaging>ear</packaging>
    <properties>
        <ear.file.name>SpringWebApplication</ear.file.name>
    </properties>
    <dependencies>
        <dependency>
            <groupId>com.sternkn.spring.multcontext</groupId>
            <artifactId>app-infrastructure</artifactId>
            <version>${project.version}</version>
            <type>jar</type>
        </dependency>
        <dependency>
            <groupId>com.sternkn.spring.multcontext</groupId>
            <artifactId>app-rest</artifactId>
            <version>${project.version}</version>
            <type>war</type>
        </dependency>
        <dependency>
            <groupId>com.sternkn.spring.multcontext</groupId>
            <artifactId>app-ws</artifactId>
            <version>${project.version}</version>
            <type>war</type>
        </dependency>
    </dependencies>
    <build>
        <finalName>${ear.file.name}</finalName>
        <outputDirectory>${project.build.directory}/${project.build.finalName}</outputDirectory>
        <resources>
            <resource>
                <directory>${resources.dir}</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-ear-plugin</artifactId>
                <version>3.0.1</version>
                <configuration>
                    <displayName>${ear.file.name}</displayName>
                    <defaultLibBundleDir>lib</defaultLibBundleDir>
                    <generateApplicationXml>false</generateApplicationXml>
                    
                    <!-- https://maven.apache.org/plugins/maven-ear-plugin/examples/skinny-wars.html -->
                    <skinnyWars>true</skinnyWars>
                    <archive>
                        <compress>true</compress>
                    </archive>
                    <modules>
                        <jarModule>
                            <groupId>com.sternkn.spring.multcontext</groupId>
                            <artifactId>app-infrastructure</artifactId>
                            <bundleFileName>app-infrastructure.jar</bundleFileName>
                        </jarModule>
                        <webModule>
                            <groupId>com.sternkn.spring.multcontext</groupId>
                            <artifactId>app-rest</artifactId>
                            <bundleFileName>AppRest.war</bundleFileName>
                        </webModule>
                        <webModule>
                            <groupId>com.sternkn.spring.multcontext</groupId>
                            <artifactId>app-ws</artifactId>
                            <bundleFileName>AppWS.war</bundleFileName>
                        </webModule>
                    </modules>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
